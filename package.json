{
  "scripts": {
    "test": "npx hardhat test",
    "testc": "npx hardhat test test/Calendar.ts",
    "comp": "npx hardhat compile",
    "dp-gr": "npx hardhat run --network goerli deploy.ts",
    "dp-hs": "npx hardhat run --network holesky scripts/deploy.ts",
    "vrf-hs": "npx hardhat verify --network holesky"
  },
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
